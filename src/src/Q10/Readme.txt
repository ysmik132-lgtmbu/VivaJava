Bhudev katawal BIT Section "P".
116237


This program demonstrates encapsulation in Java. The BankAccount 
class has a private variable balance, which is accessed only through
methods. The withdraw() method ensures that the withdrawal is valid 
and prevents overdrawing. The getBalance() method is used to retrieve
the current balance. This ensures data security and controlled access.

