A medical records system logger that writes startup messages to a daily log file using FileWriter in append mode.
The program implements robust error handling with try-catch blocks and ensures resources are properly closed, 
automatically creating new files if missing or appending to existing ones without data loss.

